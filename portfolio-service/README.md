# portfolio-service

TypeScript + Express scaffold with PostgreSQL connection.

- Dev: `npm run dev`
- Build: `npm run build`
- Start: `npm run start`
 - Prisma: `npx prisma generate` (after `npm install`)
